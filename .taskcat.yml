project:
  name: pipeline-dashboard
  regions:
    - ap-southeast-2
tests:
  test-cfn:
    template: ./test-cfn.yml
  # cloudwatch-dashboard:
  #   parameters:
  #     PipelinePattern: "*"
  #     BucketName: pipeline-dashboard-ACCOUNTID
  #     CodeKey: pipeline-dashboard.zip
  #   template: ./template.yml
